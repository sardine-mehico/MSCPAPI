# Python
__pycache__/
*.py[cod]
venv/
.venv/

# All session state: persistent browser profile (cookies, Cloudflare
# clearance, sign-in) + cached auth token. Secret.
session/
docs/

# Local config / secrets
config.json
credentials.md

# Keep the repo opaque (no project description on GitHub)
CLAUDE.md
MANUAL.md

# Dev files
test.py
main.py